class GroupMember {
  constructor(request) {
    this.name = `GroupMember`;
  }
/*
  validateUser(req){
      lookup req.user and lookup req.password in dabase;
        return rightio;
      else;
        return wrong
  }
  */
}

module.exports = {
  GroupMember,
};

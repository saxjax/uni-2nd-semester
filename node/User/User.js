class User {
  constructor(request) {
    this.name = `User`;
  }
/*
  validateUser(){
      lookup req.user and lookup req.password in dabase;
        return rightio;
      else;
        return wrong
  }
  */
}

module.exports = {
  User,
};

/* eslint no-undef: 0 */

Troll();
function Troll() {
  const listen = document.getElementById(`listen`);
  setTimeout(() => appendDomNode(`P`, listen, `Lave en gruppe med dine medstuderende!`), 1500);
  setTimeout(() => appendDomNode(`P`, listen, `Oprette dit projekts kapitler eller andre dokumenter!!`), 3000);
  setTimeout(() => appendDomNode(`P`, listen, `Tilføje de afsnit som udgør kapitlerne eller dokumenterne!!!`), 4500);
  setTimeout(() => appendDomNode(`P`, listen, `Lave EVALUERINGER til dine afsnit så også andre end dig fatter det!!!!`), 7000);
  setTimeout(() => appendDomNode(`P`, listen, `OG... TIL... SIDST...`), 9000);
  setTimeout(() => appendDomNode(`P`, listen, `SÅ KAN DU TESTE DIN VIDEN MED EVALUERINGERNE!!!!!!!!!!!!`), 12000);
  setTimeout(() => appendDomNode(`P`, listen, `wow`), 13000);
}

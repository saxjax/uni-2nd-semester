<!DOCTYPE html>
<html>
    <head>
        <%- include('meta/header') %> 
        <link href="/css/design.css" rel="stylesheet">
        <link rel="stylesheet" href="/css/elementlist.css">
        <title>GroupHub | Document </title>
    </head>
    <body>
        <%- include('meta/navbar') %> 
        <div class="container" id="content">
          <div class="row">
            <div class="col">
              <h2> Afsnit </h2>
              <div class="col-6-sm">  
                <% data.sections.forEach((section) => { %>
                    <a href="/view/section/<%= section.idSection %>">
                      <%= section.number %> <strong><%= section.title %></strong>
                    </a>
                    <p><%= section.teaser %></p>
                      <p><strong>Nøgleord: </strong><%= section.keywords %> </p>
                <% }); %>
                </div>
                <div class="col-6-sm">  
                  <button class="btn btn-info">
                    <a href="/post/section/<%= data.document[0].idDocument %>"> Opret Afsnit </a>
                  </button>
                </div>
              </div>
            </div>
          </div>  
        </div>
    <%- include('meta/footer') %> 
  </body>
</html>   